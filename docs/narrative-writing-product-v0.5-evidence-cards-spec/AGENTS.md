# AGENTS.md

1. Product V0 是基础权威规格，本目录只增加 F04。
2. 不修改 engine V1/V1.2 内部协议；能力放在产品 engine adapter 后。
3. Prompt 必须外置，结构化输出必须校验。
4. 只使用 Task 显式关联的 Source，禁止联网或隐式知识库。
5. 服务端必须重新核验正文和素材引文，模型判断不能直接成为可信锚点。
6. 内容超限时明确拒绝，不得静默截断后声称检查完整。
7. 用户确认只表示已核查这张关联卡，不表示真实性认证。
8. 修订复用现有 Patch/Version 安全规则。
