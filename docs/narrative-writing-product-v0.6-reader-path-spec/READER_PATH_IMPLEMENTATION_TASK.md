# Reader Path Implementation Task

## Objective

为当前实际正文提供只读逐段路径，并把有可靠引用的路径问题转成可控制的修订工作单。

## Work Order

1. R0 固定样例与继续开发门槛。
2. R1 增量规格、schema、外置 prompt。
3. R2 Mock/Real adapter。
4. R3 数据库 v6、持久化、过期和备份兼容。
5. R4 API 与 F03 revision item/Patch 接入。
6. R5 “原定路径 / 稿件检查”视图、导航和状态。
7. R6 自动化、浏览器 B16、报告与进展记录。

## Completion

创建 `docs/reports/PRODUCT_V0_6_READER_PATH_IMPLEMENTATION_REPORT.md`，明确工程验收与真实模型效果验证的区别。
