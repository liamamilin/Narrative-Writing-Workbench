# 开发进展记录

开始日期：2026-09-13。关联：[已完成并归档的详细开发计划](../archive/2026-09-13/V0_1_STABILIZATION_DEVELOPMENT_PLAN.md)。

记录规则：每个里程碑和重要验证后追加日志；长阶段至少每 30 分钟更新；结束工作前更新当前状态与下一步。日志记录实际完成和证据，不把计划当成完成。

## 当前状态

| 里程碑 | 状态 | 说明 |
|---|---|---|
| S00 实施评审 | 完成 | 保存基线，记录契约及旧稿入口规格冲突决定 |
| S01 干净测试基线 | 完成 | 合成 fixture、测试依赖、源码隔离入口与 CI 配置；远端 CI 持续通过 |
| S02 提交安全 | 完成 | 事务、revision、迟到写入、锁变化与配置原子性回归通过 |
| S03 版本来源 | 完成 | plan/restore 来源、当前角度与过期 review 回归通过 |
| S04 运行恢复 | 完成 | 生命周期、后端重建与浏览器强制杀进程/同库重启/手动重试均通过 |
| S05 设置与反馈 | 完成 | 手填模型、清单失败、异步覆盖及质量字段展示已验证 |
| S06 浏览器验收 | 进行中 | B00–B19 自动化通过；原生输入法、常用窗口和完整 Tab 顺序人工走查待做 |
| S07 效果基线 | 进行中 | 固定真实模型 12/12 工程运行及匿名评审包完成；人工评价及历史盲评待回收 |
| S08 迁移与交付 | 进行中 | 临时库迁移/回退演练与本轮报告完成；总放行依赖上述剩余验收 |
| F01 E0 导出/备份规格 | 完成 | V0.2 增量规格、API/包契约、验收标准和实施评审已记录 |
| F01 E1–E2 文本导出 | 完成 | 当前稿/任意 Version 的 md/txt 下载及 UI 完成；307 项与 B11 通过 |
| F01 E3–E6 备份恢复 | 完成 | 一致性备份包、预检、独立恢复、10 项专项与 B12 通过 |
| F02 A0–A5 写前确认卡 | 完成 | 不写正文的候选发现、不可变确认、过期校验、确认后生成、UI 与 B13 完成 |
| F03 W0–W6 修订工作单 | 完成 | schema v4、精确锚定、保留原文、Patch 状态事务、v3/v4 备份兼容、UI 与 B14 完成 |
| F04 E0–E6 论点与素材依据卡 | 完成 | schema v5、逐字依据与 Source 定位、用户确认、过期/修订安全及 B15 完成 |
| F05 R0–R6 稿件读者路径 | 完成 | schema v6、逐段路径、问题定位与安全修订及 B16 完成；真实效果待评估 |
| F06 I0–I5 本地选题箱 | 完成 | schema v7、SQLite 收藏、幂等旧库迁移、搜索/备注/任务关联、备份与 B17 完成 |
| F10 S0–S6 文章分享 | 完成 | schema v8、不可变快照、可撤销链接、PNG 卡片、独立阅读页、备份与 B19 完成 |

## 日志

### 2026-09-13 — 启动

- 基线：main / `72304ad`；原有修改包含 README、app.js、规划文档及 `.commandcode/`。
- 将已有 README/app.js 差异保存为 `.scratch/development_baseline_20260913.patch`；保留用户工作，不将无关内容混入开发。
- 已在 `PRODUCT_IMPLEMENTATION_REVIEW.md` 记录事务、revision、版本来源、运行状态与测试决策。
- 本机环境：Python 3.10.13、Node 22.21.1；上一轮基线 270 项测试通过，但纯源码副本存在私人 fixture 依赖失败。
- 下一步：完成 S01，加入真实故障/并发回归，再修 S02。

### 2026-09-13 12:10 — 第一轮实现与专项回归

- 新增事务句柄、Draft revision、版本来源、恢复来源、Review 内容/配置绑定和生成结果留存。
- 接受、拒绝、autosave、checkpoint、restore 与生成提交纳入一致性保护；替换前为未 checkpoint 的手工正文保留版本。
- 前后端已接入 revision；旧/缺失 revision 请求安全拒绝。
- 原有产品/Quick Write/续跑测试：103 passed；新增故障注入、并发、迟到写入、恢复来源、迁移测试：13 passed。
- 首次纯源码验证发现新增 fixture 代码缺少顶层 Path 导入，已修正，正在重新验证。
- 设置页不再将建议清单当作白名单；保留手填模型并忽略过期响应。
- 下一步：运行生命周期与浏览器验收；尚未调用真实模型或改动用户数据库。

### 2026-09-13 12:24 — 运行生命周期与干净环境验证

- 新增 writing_operations 与阶段事件记录；同任务通过数据库原子占用，取消按 300 秒判断运行已死的做法。
- 运行固定适配器与模型/提示词指纹；重试保留失败历史，并在输入变化时失效续跑节点。
- 刷新通过 operation_id 查询与重连；SSE 不再在五分钟后强制断开，旧运行事件不混入新运行。
- 服务入口增加单实例锁；启动只标记旧进程运行中断，不自动重放模型请求。
- 数据迁移改为显式事务，升级前使用 SQLite backup 保存一致副本，失败整体回滚。
- 专项事务/生命周期/迁移：21 passed；源码隔离全量测试：291 passed（27.22 秒）。
- 浏览器套件已开始执行：B01 素材写作/保存/刷新、B02 快速写作/换角度/同角度重写已通过，其余继续验证。
- 增加保存冲突的本地恢复副本与重新载入路径；原生中文输入法仍需后续人工验证，合成 composition 事件测试不等同于真实输入法。

### 2026-09-13 12:36 — 首轮工程交付

- 最终源码隔离验证：295 passed / 27.45 秒，JS 语法检查通过。
- 浏览器 B01–B10 核心/合成场景通过，已检查桌面截图；不能据此宣称原生中文输入法或完整浏览器进程重启验收完成。
- 浏览器新增发现已修复：旧稿导入未创建正文；恢复后的异步版本页面覆盖新路由。追加了原稿原子导入、旧库可证明来源回填与失败回滚测试。
- 补充保护项变化后旧提案拒绝接受、目标配置校验失败不产生部分写入两个回归。
- 12/12 合成案例完成 mock 演练；人工评价留空，usage 未知，未调用真实模型。
- npm 官方仓库安装验证通过，已生成锁文件；CI 已纳入 Python、浏览器和 mock 评估入口，远端尚未运行。
- 新增 [本轮实施报告](V0_1_STABILIZATION_IMPLEMENTATION_REPORT_2026_09_13.md)、[API 与数据契约](../planning/STABILIZATION_API_AND_DATA.md)，同步 README 与 Quick Write 评审。
- 保留全部既有用户改动；未提交 Git、未修改用户数据库和 settings；测试服务使用临时数据并在结束后关闭。
- 下阶段：先补完整浏览器重启与原生输入法验收，再补 S07 完整调用记录并执行固定配置真实评估。F01–F09 仍为待规格化的产品提案。

### 2026-09-13 12:54 — 跨进程恢复与操作观测

- B07 已改为完整进程故障演练：生成中 SIGKILL 独立 mock 服务，使用同一临时数据库和端口重启，旧 operation 变为 interrupted；页面显示中断且不自动收费重跑，用户点“重试（继续）”后新 operation 成功。
- 修复中断后重试入口：按原 operation 类型恢复 generate、regenerate 或 rediscover-angle 请求，保留已有正文；跨进程浏览器 B01–B10 再次全部通过，未捕获页面异常。
- 数据库升级到 schema v3，增加 operation_records。模型调用只记录产品阶段名、调用类型、模型、状态、耗时和提供方实际返回的 usage；不保存提示词、输入、输出或密钥。未知 usage 保持 NULL，并在汇总中标记 incomplete。
- RealWritingEngine 记录意义发现每次尝试、命题评审、结构、语言检查、输出门和审阅结论；局部补丁也纳入独立 operation。评估脚本导出成功与失败 operation 的事件、记录、耗时和 usage。
- 新一轮 12/12 mock 案例完成，共导出 24 个 operation；mock adapter 没有提供方 usage，24 个均如实标为未知，未伪造零 token。人工评分仍为空，未调用真实模型。
- 当轮源码隔离验证：298 passed / 28.00 秒；JS 语法、Python 编译和 diff 格式检查通过。失败调用同样记录模型名且不包含请求正文的回归已覆盖。
- 新增 [浏览器人工验收表](../../tests/browser/MANUAL_ACCEPTANCE.md)。剩余关键项为原生中文输入法/长文走查、固定真实模型 12 例及人工效果评分；F01–F09 尚未开始实现。

### 2026-09-13 13:22 — 真实模型基线

- 评估器增加显式 `--settings` 凭据来源、固定模型/超时/最大重试覆盖、容量试跑限制和 `RUN_METADATA.json`；settings 只提供 API Key 与 Base URL，不复制到结果目录。
- 首次固定配置 `mimo-v2.5` 与当前 Ollama 端点不匹配，12/12 由端点立即返回 model not found；失败 operation 全部留存。由此发现模型配置漂移。
- 本机 `qwen3.8:27b-mlx` 首次结构化调用运行 460.36 秒仍未返回，人工终止容量试跑；记录暴露 SDK 默认重试会让总等待隐式超过角色超时，评估器现默认 `max_retries=0`。
- `qwen3.5:9b-mlx` 单例试跑中意义发现经修复完成，但命题评审和结构调用在 120 秒超时；Q01 在 372.17 秒后失败，不扩展批次。
- `gpt-oss:120b-cloud` 的 1 例容量试跑通过，随后固定该模型、120 秒超时、零重试执行完整 12 例：12/12 completed，24/24 operation succeeded，40/40 模型调用完成。
- 12 例累计 151.71 秒，中位 11.31 秒，范围 3.34–24.58 秒。提供方为 32 次调用返回 usage：83,923 input tokens、30,085 output tokens；8 次流式正文调用 usage 未返回并保持未知，不能据此计算完整 token 或费用。
- 8 个生成 operation 的结构、语言检查和输出门全部通过；4 个 Quick Write 命题评审接受。8 篇生成稿检查结论均为 PASS；4 篇旧稿均为 PATCH_REQUIRED，并完成提案、只改目标段落和人工接受流程。
- 结果中未发现 API Key、Authorization、Bearer 或常见密钥前缀。人工质量评分尚未填写，工程成功不能解释为写作质量已经证实。
- 新增 [真实模型评估报告](REAL_MODEL_EVALUATION_REPORT_2026_09_13.md)及[人工评分表](REAL_MODEL_EVALUATION_HUMAN_REVIEW_2026_09_13.md)。剩余关键项为人工质量评分、原生中文输入法/长文验收及历史 V1.2 盲评。
- 评估器重试边界变更后的最终源码隔离回归：298 passed / 27.36 秒；没有读取用户数据库，也没有改写 `workbench/settings.json`。

### 2026-09-13 13:47 — GitHub 交付与 Linux CI 竞态修复

- 已创建并推送 `codex/v0-1-stabilization` 到 `git@github.com:liamamilin/Narrative-Writing-Workbench.git`；主实现提交为 `56f2f29`，浏览器失败诊断提交为 `1dbd35e`，远端对应 PR #2。
- 首轮 push/PR 检查中，Python、Node、依赖安装和 Chromium 安装均通过；浏览器 B01、B02 通过，B03 等待 `#f-instruction` 超时。诊断输出现包含具体场景和 GitHub Actions 错误注解。
- 根因是 B02 后端版本更新后，前端仍可能处于 `reloadTask()`：测试进入旧稿修改入口后，旧任务回调继续执行 `renderWorkspace()`，覆盖了新路由的表单。慢速 Linux CI 稳定暴露了这个窗口。
- `runGeneration()` 与刷新后续接流程现会在异步任务重载前后都核对任务和路由；离开页面后旧回调不再重绘当前页面。
- 修复后源码隔离测试 298 passed，JavaScript 语法和 diff 检查通过；独立浏览器 B01–B10 全部通过。下一步推送修复并观察远端 push/PR 两组检查。
- 修复提交 `1d0600b` 已推送；GitHub Actions push 运行 #5（1 分 32 秒）与 PR 运行 #6（1 分 41 秒）均完成并通过，Linux CI 闭环完成。
- 仅修改通过记录后的同代码复验中，push 运行 #7 通过，PR 运行 #8 在 B07 重启后的手动重试等待 15 秒后超时。该流程包含强制杀进程、同端口重启和浏览器重新连接，15 秒不足以覆盖共享 runner 波动；B07 单独改用 30 秒上限，并在重试进入失败/中断或超时时输出最新 operation，避免仅留下无状态的超时信息。

### 2026-09-13 14:13 — B09 长文本与段落范围收口

- 自动化新增 5,200 字中文剪贴板粘贴，核对目标段落、其余段落、服务端正文及刷新后正文完全一致；composition 期间仍不提前保存，上屏后再提交。
- 浏览器走查暴露并修复选区语义：段内/跨段 DOM 文字选择现映射到相交段落；反向 Shift 不再漏选前段；Shift、⌘、Ctrl 都扩展为可见的连续范围，不会出现只高亮 1、3 段却实际提交 1–3 段的差异。
- Review 定位与 Writing Map 定位会清理旧选择、设置新锚点，并在返回正文后显示选区工具栏；待处理提案按创建时间稳定返回，刷新后顺序确定。
- 浏览器 B01–B10 全部通过；新增断言核对跨段提案提交范围为 ¶2–3、接受前正文不变。源码隔离测试 298 passed / 27.65 秒，JavaScript 语法与 diff 检查通过。
- S06 仍保留原生中文输入法和完整键盘人工走查，不把自动化剪贴板与合成 composition 当作人工验收。

### 2026-09-13 14:16 — B10 键盘路径与焦点恢复

- 对话框现在记录触发前焦点，确认或 Escape 关闭并移除后将焦点还给仍在页面中的触发控件；自动化验证生成确认框 Escape 后焦点回到 `#p-gen`。
- Quick Write 话题卡的 `role=button` 现与行为一致：聚焦后 Enter 或 Space 都会选中话题、填入输入框并把焦点移到可继续编辑的位置。
- Writing Map 的结构卡片支持 Enter/Space 激活；自动化使用 Enter 返回正文并确认目标段落及选区工具栏可见。
- B01–B10 再次全部通过，JavaScript 语法和 diff 检查通过。真实中文输入法、完整 Tab/Shift+Tab 顺序及中断后纯键盘恢复仍留在人工验收表。

### 2026-09-13 14:32 — 产品效果匿名评审闭环

- 新增 `scripts/product_human_review.py`：把工程结果与固定案例合并为匿名评审正文，按显式 seed 稳定重排，并将原案例/入口映射单独保存在评审包目录之外。
- 公开评审包排除模型名、自动 PASS/PATCH_REQUIRED、运行状态、耗时、review/task/operation 标识；保留判断质量所需的题目、素材、要求、正文和局部补丁前后文。
- 汇总阶段要求具名评审者、带时区的 ISO 8601 时间、完整适用维度和 1–5 整数分；来源 hash、packet id、案例集合或维度不一致时拒绝产出报告，不会把空值变成零分。
- 已从真实 12 例生成本地 packet `db7c1f2964ed9bdbf8a5`；公开目录扫描未出现模型名、自动结论、operation 或 Q/S/D 原编号，评分模板保持为空。
- 新增 4 项测试，覆盖固定 seed 可复现、盲化字段隔离、私有 key 路径保护、具名评分汇总、空分/越界/错误维度/失败案例拒绝。源码隔离全量回归 302 passed / 27.42 秒，Python 编译、JavaScript 语法及 diff 检查通过；实际人工分数和历史 V1.2 评审仍待真实评审者回收。

### 2026-09-13 14:54 — F01 第一段：本地文本导出

- 新建 Product V0.2 导出与备份增量规格，记录与 V0 停止规则/Publishing 非目标的关系、E0–E6 里程碑、文本 API、备份包目标和验收标准；本轮按用户明确的新功能开发要求进入 E0–E2。
- 新增 `workbench/exporting.py`，只接收标题、正文与标识，生成 UTF-8 Markdown/纯文本和安全下载文件名；普通文章导出无法接触素材、WIR、审阅、operation、模型或 settings。
- 当前稿导出会等待 composition 和所有 autosave，在数据库临界区核对最新 revision；缺失/非法 revision、并发修改或保存失败均停止下载。指定 Version 导出只读，不触发 Restore。
- 工作台标题栏提供格式选择和“导出当前稿”，版本历史每行可按共享格式下载。1280×800 截图复核后正文仍占主区域，控件没有遮挡关键操作。
- 新增 5 项服务/API 回归；源码隔离全量 307 passed / 28.51 秒，Python 编译、JS 语法及 diff 检查通过。
- 浏览器首次在受限环境未能启动临时服务；允许本地端口/Chrome 后 B01–B10 通过，B11 首次发现测试只改第一段却错误期待整篇仅剩一段，修正为完整正文断言后 B01–B11 全部通过。导出保留其余段落、不新增版本，历史版本内容逐字一致。
- E1/E2 完成。下一步实现 E3–E5：SQLite 一致性快照、manifest/hash、损坏包预检，以及只恢复到不存在的独立路径。

### 2026-09-13 15:20 — F01 第二段：工作区备份与独立恢复

- 新增 `workbench/backup.py`：用 SQLite backup API 生成一致性快照，备份包固定只含 `manifest.json` 与 `workspace.sqlite3`，并记录 schema v3、字节数、SHA-256 和 15 张表的对象数。
- WAL 源库的已提交数据能进入快照；目标快照统一为单文件 DELETE journal，不依赖 `-wal/-shm` 边车文件。settings、API Key、env、日志、评估和浏览器存储不进包。
- 预检覆盖 ZIP 固定文件集/压缩方式/大小、manifest、hash、SQLite integrity/schema/表列、对象数与跨表引用；拒绝路径条目、篡改、未来 schema、断链和超限包。
- 正式恢复不接受客户端目标路径，只在受控 `restored/` 下创建随机新目录；临时目录再校验后 rename，任何失败清理半成品，当前数据库不变。
- 设置页新增“本地数据”卡片：下载、选择包、预检、确认恢复为四个可见阶段；更换文件会废弃旧预检状态。
- 备份专项 10 passed；源码隔离全量 317 passed / 28.94 秒；JS 语法和 diff 检查通过。桌面 Chrome B01–B12 全部通过，B12 完成下载→预检→独立恢复往返并核对当前 Draft 不变。
- F01 E0–E6 完成。下一候选是 F02 写前确认卡；开发前需先把候选角度、编辑字段、输入过期和确认快照写成增量规格。
- 实现提交 `8160170` 已推送；GitHub Actions push #19（1 分 39 秒）与 PR #2 #20（1 分 35 秒）全部通过。

### 2026-09-13 — F02 写前角度确认

- 新建 Product V0.3 增量规格和实施评审，固定直接写旁路、候选安全字段、完整编辑、不可变确认、输入过期和失败续跑契约；数据库继续为 schema v3。
- 新增 angle-options 查询/生成与 confirm-angle API。候选发现是一项独立 operation，只写 meaning discovery；不创建 Draft、Version 或 plan。未确认候选不会成为 current meaning。
- 确认会追加 `confirmed` meaning，保存父 discovery、candidate、选择来源和输入指纹。生成只接受当前任务、当前输入的确认快照；跨任务、预览用途和过期快照被拒绝。
- Quick Write 新增“先看角度”、候选卡、完整五字段编辑、“再找一批”和“确认并开始写”；表单变化会废弃页面候选，原“开始写”保持不变。
- 专项 Quick Write/V0.3 80 项通过；全量 328 passed / 约 30 秒。Chrome B01–B13 全部通过，B13 验证预览无正文、键盘选择、编辑同步、确认后生成和最终 meaning。
- mock 验证只能证明编排与失败安全；真实模型候选质量、总等待、写后换角度次数和最终保留率仍需真实试用收集。
- 实现提交 `26c26c7` 已推送到 PR #2；GitHub Actions push #23（1 分 46 秒）与 PR #24（1 分 40 秒）全部通过。

### 2026-09-13 — F03 修订工作单与保留原文

- 新建 Product V0.4 增量规格与实施评审；固定字符区间为 Python Unicode 索引、段落为 1-based 包含区间、quote/hash/revision 三重锚定，不用模糊匹配猜测过期位置。
- 数据库升至 schema v4，新增 `revision_items` 与 `preserved_spans`，Patch 可关联工作单。严格备份预检同时接受 v3/v4；恢复的 v3 数据库由 Workbench 正常升级。
- Review 保存严重度、影响、修订目标、原文引用和精确位置，优先展示三项。工作单支持 open/proposed/resolved/dismissed/stale；拒绝提案恢复 open，跳过不改正文。
- 用户可对连续完整段落标记“保留原文”。服务端在提案入库前和 Accept 事务内两次验证；冲突时不产生提案，不相交修订接受后确定性重定位保留范围。
- Accept 在同一事务中新建 Version、接受 Patch、解决当前条目、过期其他条目并重定位保留范围；故障注入和并发回归证明失败不部分提交。
- 浏览器首轮在 B05 暴露双击接受的异步刷新可覆盖下一路由，已改为提案卡单次处理并在回调前核对原任务路由。修复后 Chrome B01–B14 全部通过，无未处理页面异常。
- 远程 push #27 / PR #28 的 Linux 检查在 B09 暴露慢速 `reloadTask()` 在后续路由重绘的竞态，已改为重载前后双重路由校验，B04 用可控延迟固定该时序。push #29–#31 / PR #30–#32 仍在 B09 取到旧的第 1 段提案：新 ID 集合差证明不是卡片顺序，而是浏览器 `go()` 只等待 hash，旧页又同样存在 `#editor`。工作区现暴露 `data-task-id`，所有任务导航在交互前等待目标任务真正渲染；新提案继续用 ID 集合差核对。
- 目标任务渲染同步修复提交 `debc4d0` 推送后，GitHub Actions push #33 与 PR #34 均完成并通过，Linux/Chromium 回归闭环。
- 只含通过记录的提交 `c44e910` 触发 push #35 / PR #36，两个 job 都在启动前因 GitHub runner 连续 5 次无法获取而失败，未执行仓库代码或测试。
- F03 专项 10 passed；全量 338 passed / 30.32 秒；纯源码副本 338 passed / 30.11 秒；Python/JavaScript 语法检查通过。真实模型诊断优先级、修订目标质量和真实修稿耗时尚需实际试用数据。

### 2026-09-13 — F04 论点与素材依据卡

- 新建 Product V0.5 增量规格和实施评审，限定为用户已提供材料的 source-grounded 分析/观点写作；不联网，不把模型判断或用户确认包装成事实认证。
- 产品 adapter 新增有界 `check_evidence()`；Real 使用外置 prompt 和 JSON Schema，Mock 保持同形。正文、Source 数量、材料总量和卡片数量均有明确上限，超限返回可操作错误。
- 数据库升至 schema v5，新增 `claim_checks`、`claim_links` 和 Patch 的 `claim_link_id`。严格备份预检支持 v3/v4/v5，并检查依据卡到 task/draft/source/operation/patch 的关系。
- 服务端逐字核验正文和 Source 引文及唯一位置；错误 Source 或虚假材料引文不能显示“材料支持/冲突”，而是降为证据不足。正文无法精确定位的结果不创建可操作卡。
- Source 集合与内容 hash、Draft revision/hash 组成检查快照；正文或素材变化后旧检查动态过期，确认、忽略、提案和接受均会重新校验。
- 右侧检查页新增正文—依据卡—素材定位、独立用户确认/忽略和局部处理；提案复用 F03 的 Before/After、Accept/Reject 与 Version 安全。
- F04/F03/备份/运行事务专项 42 passed；全量 349 passed / 31.34 秒，纯源码副本 349 passed / 33.15 秒；Chrome B01–B15 全部通过且无未处理页面异常。
- 工程验收不能证明真实模型的支持关系判断质量。F04-7 的错误支持、遗漏和用户核查耗时仍待真实样本评估。

### 2026-09-13 — F05 读者路径检查

- 新建 Product V0.6 增量规格、离线可行性门槛和实施评审；固定样例覆盖逐字重复、无中间依据的结论和提出后未回答的问题，产物必须增加完整逐段路径，不能只是改写现有 Review 评分。
- adapter 新增有界 `review_reader_path()`；Real 使用外置 prompt/schema，Mock 保持同形。服务端要求每个非空段落恰好一个逐字匹配步骤，遗漏或重复使整轮失败；问题 quote 无法唯一定位时不创建工作单。
- 数据库升至 schema v6，以 `reviews.analysis_type` 隔离普通写作检查和稿件路径检查，新增 `reader_path_steps`，路径问题复用 F03 `revision_items`。严格备份预检支持 v3–v6，并验证步骤、Review 与 Draft 关系。
- 工作台中栏将旧 Writing Map 明确命名为“原定路径”，新增“稿件检查”双栏视图。逐段路径和问题都可定位正文；问题可跳过或生成 ProposedPatch，拒绝后重开，接受后新建 Version 并使旧检查过期。
- F05 专项 10 passed，F03–F05/备份/operation 相关 52 passed；全量 359 passed / 31.65 秒，纯源码副本 359 passed / 31.97 秒。Chrome 152 / Playwright 1.62.1 的 B01–B16 全部通过，无未处理页面异常。
- 实现提交 `389e6a7` 已推送到 PR #2；GitHub Actions push #41 与 PR #42 均完成并通过。
- F05-5 仍待真实模型自然文章评估，需记录相对 Review 的新增有效问题、误报/遗漏、修订改善和等待成本。下一工程候选为 F06 本地选题箱。

### 2026-09-13 — 下一阶段计划重新排序

- 核对规格、实施报告和未完成验收后，将 B09–B16 真实浏览器人工走查列为第一优先，将 F02–F05 真实模型与人工效果决策列为第二优先；当前主要风险是效果证据缺口，而不是缺少更多入口。
- 评审材料发出后可并行推进 F06 本地选题箱。它不增加模型调用，可修复浏览器选题缓存易丢和无法关联任务的问题，是当前风险最低、准备度最高的新功能。
- F07 改为需求信号触发；F08/F09 只保留阶段 A 可行性验证，分别受 F04 支持关系质量和 F05 定位/F07 候选隔离约束，不默认连续开发。
- 详细排序、工作量、退出条件和暂停规则已记录在 `docs/planning/NEXT_DEVELOPMENT_PRIORITIES_2026_09_13.md`。

### 2026-09-13 — F06 本地选题箱

- 新建 Product V0.7 增量规格和实施评审；明确区分页面会话中的“本次生成”和 SQLite“选题箱”，未收藏候选不会自动成为长期用户数据。
- 数据库升至 schema v7，新增 Ideas、状态与任务索引。Unicode NFKC、空白折叠、trim、casefold 只合并明确相等项；不做语义去重。
- 新增创建、搜索/过滤、备注/状态更新与最多 200 条旧库原子导入 API。旧 `qw_topic_lib_v1` 只有在 imported + existing 完整覆盖 received 后才删除，失败保留原副本。
- Quick Write 可收藏当前话题或生成候选，检索与整理选题，从待写条目创建原子关联的 topic-only Task，并从已写条目打开原文章。没有关联 Task 的条目不能标为已写。
- 严格备份支持 v3–v7，v7 包含 Ideas 并校验状态、Task 存在与 topic-only 关系；v6 文件升级前创建 `.pre-v7-*` 一致性备份。
- F06 专项 10 项、全量 Python 369 项通过。Chrome 152 / Playwright 1.62.1 的 B01–B17 全部通过；B17 覆盖迁移、候选不落库、收藏、搜索、备注、清空 localStorage、跨重载、任务关联与打开原任务。
- 实现提交 `7343e25` 已推送；GitHub Actions push #47 与 PR #48 均完成并通过，远端覆盖源码隔离、Linux Chromium 浏览器回归和产品评估。
- 工程实现完成。下一步回到 B09–B17 人工走查和 F02–F05 真实模型效果评审；F07 只在真实使用显示两稿对照需求时启动。

### 2026-09-13 — 按核心体验重新排序

- 以“话题/角度 → 初稿 → 安全编辑保存 → 发现问题 → 用户确认局部修订”为核心主链路，重新标注未完成工作对体验的影响。
- 将基础初稿、F02 写前角度、真实中文输入与保存恢复、F03 局部修订列为 C0；导出/备份/选题持久化与 F04 材料依据列为 C1；F05、F07 为实验或条件能力，F08/F09 后置。
- 下一开发工作固定为核心质量评审工具和匿名评审包；人工走查先执行 B09/B10/B13/B14，再执行 B11/B12/B17。F04、F05 分开评价，避免综合均分掩盖错误支持或无增量诊断。
- 已同步更新下一阶段优先级、规划入口、新功能批次表、人工验收分组和产品介绍中的选题箱说明。

### 2026-09-13 19:00 — 真实任务超时卡点与生成反馈修复

- 诊断用户现场任务 `task_d49376c6765f`：operation `op_a3642e0e35ee` 停在 `meaning_discovery`，本地 `qwen3.8:27b-mlx` 没有返回结构化结果，最终抛出 `openai.APITimeoutError`；没有创建 Draft 或 Version，失败安全成立。
- 设置中的单次超时为 300 秒，但 OpenAI SDK 默认又重试 3 次，实际耗时 1,203,194 ms。此前评估器已固定 `max_retries=0`，Workbench 产品适配器遗漏了同一约束；这次现场任务证明它是 C0 主链路阻断问题。
- `RealWritingEngine` 的产品默认改为零隐式重试；用户点击“重试”仍是明确、可控的重试入口。一次卡住调用现在按配置的单次上限结束，不再把 300 秒静默放大为约 20 分钟。
- 服务层识别 SDK/httpx/Python 超时链，进度事件和 API 返回同一条中文可操作信息，包含本次快照中的超时值；生成、检查也复用同一处理，避免先发笼统错误掩盖真实原因。
- 页面刷新并重新接入运行时，从 operation `started_at` 恢复累计耗时；排队步骤改为“已开始”，超过 180 秒时说明会按设置中的单次超时停止。
- 专项 94 项、全量 371 项通过，Python/JavaScript 语法与 diff 检查通过；Google Chrome 152 下 B01–B17 全部通过。修复后的 real Workbench 已重新启动，现场任务仍为 failed、Draft 为空。本轮没有重试真实模型，也没有修改任务正文或用户设置。

### 2026-09-13 19:21 — 更换 API/模型后的现场复查

- 用户将设置切换到 OpenCode Go 与 `mimo-v2.5` 后，原任务以 `op_8331ea0fb108` 续跑，operation 正确引用前一次失败；新 API、Key 和模型均被本次运行采用。
- 这次不是连接或超时失败。首轮 meaning discovery 在 208.38 秒返回 1,955/6,000 tokens；结构修复在 125.69 秒返回 2,011/6,000 tokens。两轮都达到 6,000 输出上限，operation 在 334,087 ms 后以 `DISCOVERY_FAILED` 结束。
- 内存 debug 流显示修复结果只生成到第一个候选角度标签开头，随后用大量空白耗尽输出上限；`json.loads` 在字符 21,139 后报告缺少分隔符。当前角色已经使用 `reasoning_effort: low`、`structured_mode: auto`，因此这是 `mimo-v2.5` 在该长结构 JSON 请求上的兼容/稳定性问题，不是 Workbench 卡死。
- Draft、Version 和未应用结果仍为空，失败安全再次成立。没有继续自动重试。下一步应先换用已通过完整评估的模型，或单独实现结构调用的 finish-reason/截断诊断与模型兼容策略后再试 `mimo-v2.5`。

### 2026-09-13 19:29 — DeepSeek 现场成功对照

- 用户在同一 OpenCode Go API 下把模型切换为 `deepseek-v4-flash`，同一任务以 `op_c055409816c3` 再次续跑并成功，证明前一轮问题不是 API、Key、任务数据或 Workbench 编排故障。
- 意义发现 69.88 秒、命题评审 29.79 秒、结构 103.93 秒、正文 55.12 秒，总耗时 258,761 ms。三个结构化阶段均一次通过，结构未触发 repair；正文中文检查与输出门一次通过。
- 任务进入 `ready`，创建 Draft `draft_dfa79137bad3` revision 1 和 generation Version `ver_f1b3d4b9c888`。本次 6 段正文已安全提交。
- 这组同 API、同任务的直接对照把当前兼容性结论收窄到模型：`deepseek-v4-flash` 可完成主链路；`mimo-v2.5` 在 Meaning Discovery 长 JSON 上两次达到输出上限并产生截断结果。后续应把结构化稳定性列入模型准入检查。

### 2026-09-13 19:38 — 当前计划确认与历史文档归档

- 规划入口明确当前顺序：P0-1 核心质量评审、P0-2 核心交互走查、现场缺陷修复、F04/F05 分项决策，再按证据决定 F07–F09。
- 将 HEAD `72304ad` 的初始项目审计、已完成的 S00–S08 稳定性计划和已失去执行效力的旧引擎未来路线图移入 `docs/archive/2026-09-13/`；新增总归档索引和日期索引，说明归档原因与当前替代入口。
- 保留仍有执行或审计价值的当前优先级、新功能 F07–F09 计划、产品演进路线、人工评审协议、稳定性数据/API 契约以及各版本实施报告。
- 更新 MANIFEST 和所有仓库内引用；扫描 `docs/**/*.md`、README 与 AGENTS 的本地 Markdown 链接，无断链。归档只移动历史资料，不改变规格权威关系或产品代码。

### 2026-09-13 23:02 — 桌面 App 图标重设计

- 用新的无文字图形替换旧“深绿底、纸张、金色笔尖”方案：米白书页代表写作工作台，连续墨迹代表意义推进，朱砂方印代表完成节点；视觉颜色与当前“纸墨写作台”界面一致。
- 将 1254×1254 RGBA 源图纳入 `assets/app-icon-source.png`，透明通道保留；16–256px 缩放检查中，书页、墨迹和红色节点仍可区分。
- `scripts/make_icon.py` 改为从受版本控制的源图确定性生成 1024px ICNS，并使用绝对仓库路径，允许从任意工作目录执行。
- 已重建本地 `NarrativeWorkbench.app/Contents/Resources/AppIcon.icns`（2,234,994 bytes）并重复执行构建验证；本地 App wrapper 原本未签名，本次没有破坏已有代码签名。

### 2026-09-14 — 全部任务与安全删除

- 新增按更新时间排序的“全部任务”页，首页最近任务提供完整列表入口；任务状态改为中文显示，项目任务区复用同一任务行。
- 首页、全部任务、项目页和工作台“信息”页均提供删除入口。确认框明确列出正文、版本、检查和运行记录会永久删除；取消不写入。
- 新增 `DELETE /tasks/{task_id}`。数据层在单个 SQLite 事务中按依赖顺序清理 Draft、Version、Patch、三类检查、生成中间产物和 operation 子记录；任一步失败整体回滚。
- 运行中的任务返回 `409 TASK_RUNNING`，不尝试从另一个请求终止模型调用。工作台入口先等待自动保存，避免保存与删除竞速。
- 关联 Idea 不删除：解除 Task 后，`written` 回到 `to_write`；Project Source 保留。无 Project 且删除后没有其他 Task 引用的 Source 会清理，避免独立任务素材成为不可见孤儿数据。
- 删除规格与验收边界记录在 `docs/planning/TASK_DELETION_SPEC_2026_09_14.md`。专项 API/Idea/operation/备份/修订检查回归 86 项、完整 Python 375 项通过；Chrome/Playwright B00–B18 全部通过，B18 覆盖取消、确认、列表消失、Task 404 与关联 Idea 回到待写。

### 2026-09-14 — P0-1 核心质量评审工具 v2

- 匿名评审工具扩展为 schema v2，覆盖三类核心对象：基础初稿、F02 写前角度候选集和 F03 局部修订；v1 包和历史待评目录继续兼容。
- 角度候选在公开包中改用 `C01`–`C05` 代码，原始候选 ID 只保存在评审包目录外的私有 key；汇总时恢复选择结果，避免评审者通过编号猜到内部数据。
- 评分维度按对象隔离：初稿为命题/推进、可辩护性、保留价值；角度集为角度价值、区分度、可辩护性、选择信心；修订为目标改善、上下文保留、范围控制。
- `evaluate_product.py --include-angle-options` 可把 Quick Write 的候选集写入评估结果并指向 v2 准备命令；未启用时标准评估路径不增加角度调用。
- 限量评估的 `MANIFEST.json` 只统计实际结果案例，不把 fixture 中未运行的案例混入案例数。v2 专项测试 7 项通过，4 例 mock 端到端 smoke 生成 8 个匿名评估项；真实模型评分仍待至少两名独立评审者回收。
- 实现提交为 `773f8f2`，已推送到 `codex/human-review-v2`；`origin/main` 尚未合并本轮提交，工作区只保留未跟踪的本地 `.commandcode/` 数据。
- 下一步仍为 P0-2 核心交互人工走查，优先 B09、B10、B13、B14；质量评审完成后分别决定是否修正 F02/F03，再进入 F04/F05 分项决策。

### 2026-09-23 — Product V0.8 文章分享

- 用户确认工作台分享方案后，新建 V0.8 增量规格与实施评审；入口固定在正文工具栏“版本”之后，范围限定为文章快照、链接、卡片和独立阅读页。
- 数据库升至 schema v8，新增 `article_shares`。分享创建前完成 autosave 并校验 revision；快照不可变，更新时撤销旧 token 并生成新 token，停止分享和删除任务/项目都会使链接失效。
- 私有管理 API 与公开读取 API 分离。公开 JSON 只组装标题、正文、署名、摘要、发布时间和阅读时长，不序列化 Task/Draft/Version；HTML 转义正文并设置 noindex、no-referrer、CSP、nosniff 和 no-store。
- 分享面板交付作者与摘要、卡片预览、复制链接、系统分享、打开文章、1080×1440 PNG 下载、过期快照提示、更新和撤销。localhost 明确提示链接只在本机有效。
- 公开文章页采用独立响应式版式，桌面与手机均使用窄正文栏、中文衬线正文和清晰元信息；页面不依赖工作台路由。
- 严格备份支持 v3–v8，v7 可升级到 v8，v8 验证分享与 Task/Draft 的同任务关系；旧迁移测试同步剥离 v8 对象后继续验证原版本契约。
- 新增 5 组专项测试；全量 Python 393 项通过。Google Chrome / Playwright B00–B19 全部通过，B19 覆盖创建、公开访问、卡片下载、快照不变、更新换链和撤销 404。
- 后续按真实部署需要验证手机 Safari/Chrome 跨设备访问；可选封面图和访问范围只在出现明确需求后进入独立规格。

### 2026-09-24 — 工作台排版收口

- 根据桌面截图检查发现，主要问题不是三栏错位，而是页面、正文栏和右侧面板同时滚动，且长标题可能挤压工具栏。
- 桌面端改为工作区占满视口剩余高度，外层页面不再额外滚动；正文和侧栏保留各自必要的滚动，右侧面板标签在面板内滚动时保持可见。
- 长标题改为可伸缩并允许自然换行；960px 以下恢复页面自然滚动，避免桌面端高度规则影响平板和手机。
- Chrome 浏览器 B00–B19 全部复验通过，分享、导出、版本、检查和删除流程无回归。
